import { Card } from "../components/Card";

export function VideoPanel() {
  return (
    <Card title="Video">
      <p>Realtime video / Replay 連結於 commit 3+ 接入。當前顯示 placeholder。</p>
    </Card>
  );
}
