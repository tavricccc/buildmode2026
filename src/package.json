{
  "name": "care-agent",
  "version": "5.0.0",
  "private": true,
  "type": "module",
  "description": "Care Agent — L1 local person gate → L2 Gemini → L3 MiniMax escalation",
  "author": "Artificial Illusion",
  "license": "GPL-3.0-or-later",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "start": "bun run scripts/start.ts",
    "dev": "bun run scripts/start.ts --dev",
    "debug": "bun run scripts/start.ts --debug --stubs",
    "debug:live": "bun run scripts/start.ts --debug",
    "debug:seed": "bun run scripts/debug.ts seed",
    "debug:stream": "bun run scripts/debug.ts stream",
    "migrate": "bun run scripts/migrate.ts",
    "verify": "bun run scripts/verify.ts",
    "build": "cd frontend && bun run build",
    "probe:gemini": "python3 scripts/probe_gemini.py",
    "probe:minimax": "python3 scripts/probe_minimax.py"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.5.4"
  }
}
